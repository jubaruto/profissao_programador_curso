//###### DOM ######
//DOM (Document Object Model): É uma estrutura que representa os documentos HTML e XML. É possível alterar a estrutura HTML, utilizando JS - adicionar uma nova tag, modificar a cor de fundo de algo e etc. A estilização também pode ser modificada. A DOM é basicamente uma árvore de elementos que representam a página do browser e conseguimos acessar, por padrão, utilizando JS.
//WINDOW -> É o objeto principal, fazendo referencia a toda janela do browser. Dentro do WINDOW teremos os:
//DOCUMENT -> Faz referencia ao documento HTML;
//LOCATION -> Faz referência a URL, Path e etc;
//HISTORY -> Representa o histórico de navegação da sessão. Basicamente, é o objeto que faz o controle e contém os métodos e propriedades responsáveis pela navegação da sessão;
